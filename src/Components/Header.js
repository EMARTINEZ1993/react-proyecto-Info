import React from 'react';

/**
 * Componente de cabecera de la aplicación.
 * 
 * @returns {JSX.Element} Elemento de header con el título
 */
const Header = () => {
  return (
    <header className="app-header">
      <h1>React.</h1>
    </header>
  );
};

export default Header;